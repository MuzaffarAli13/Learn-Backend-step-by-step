export const capitalizeString = (str)=>{
    return str.toUpperCase();
}



// use export to reuse code block

// create am export fallback with export default
export default function sender(a){
    return a
}